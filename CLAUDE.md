# Save the Votes — Project Instructions

## Pre-Deploy Checklist

Before every `vercel --prod` deploy, verify the "Last updated" date in `src/components/Footer.jsx` matches today's date in the format **Month Day, Year** (e.g., "Last updated February 27, 2026"). Update it if it doesn't match.
